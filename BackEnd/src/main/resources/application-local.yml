spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/webserver_db
    username: jrdevangel
    password: iMacniaco
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

app:
  security:
    admin:
      username: admin
      password: admin123